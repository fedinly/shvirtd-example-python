SET @MYSQL_BACKUPER_PASSWORD = '$MYSQL_BACKUPER_PASSWORD'
CREATE USER IF NOT EXISTS 'backup-04'@'%' IDENTIFIED WITH mysql_native_password BY @MYSQL_BACKUPER_PASSWORD;
GRANT ALL PRIVILEGES ON virtd.* TO 'backup-04'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;
exit
